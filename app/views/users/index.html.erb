<% provide(:title, 'ユーザー一覧') %>


<h1>ユーザー一覧</h1>

<div class="row">
  <div class="search_form">
    <%= render '/layouts/search_form' %>
  </div>
</div>

<%= will_paginate %>

<% unless @users.empty? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% end %>
